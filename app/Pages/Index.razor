@page "/"
@inject IJSRuntime _jsRuntime

<h1>Hi, I'm Ben! Welcome to my page. Take a look around.</h1>

<h3>Demonstrating python</h3>
<pre><code class="lang-python" >def my_function():
  print("Hello from a function")</code></pre>

@code{
  public int EventsFired = 0;
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
      await _jsRuntime.InvokeVoidAsync("removeloader");
      await _jsRuntime.InvokeVoidAsync("Prism.highlightAll");
    }

    protected override void OnInitialized()
    {
      // Subscribing to child event not doing anything at the moment
      Contact.OnEventAdded += () => 
      {
        StateHasChanged();
      };

      Unsubscribe.OnEventAdded += () => 
      {
        StateHasChanged();
      };
    }
}